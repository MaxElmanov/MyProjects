package patterns.prototype;

/**
 * @{link Copyable} interface was created by Max to replace @{link Clonable} interface.
 * But, of course, you can use @{link Clonable} interface too.
 */

public interface Copyable {
    Object copy();
}
